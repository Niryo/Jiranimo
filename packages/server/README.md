# jiranimo

Local Node server for the Jiranimo Jira automation flow.

## Install

```bash
npm install -g jiranimo
```

## Run

```bash
jiranimo /path/to/repo-or-repo-root
```

The CLI starts the local API/MCP endpoint used by the Jiranimo browser extension.
